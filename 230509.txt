어필을 잘 해라
중요한 건 아니야 했던 거로 포장하고 잘 정리하면 됨....
숙제 내는 그런 느낌이 아니니까 완벽한 걸 바라는 것도 아니고
그냥 감상문이라도 써도 돼 일기라도 써 나는 스타일 시트랑은 안 맞는다 딴 거 공부하고 싶으면 해도 돼
교수님도 별로 안 좋아하신대
디자인 빨리 벗어나도 자바 빨리 하고싶어 과제 체크만 벗어나면 돼. 
이 랜덤이 무섭네 이게

로그같은 거 잘 남기자
디버거를 이용해서 디버깅 하는 것도 알려주신대

컴파일

인터프리트 언어는 컴파일 안합니다 그냥 실행이 돼 한줄씩

이런 애들의 특징은 장단점이 있어서 한 줄 씩 실행하는 애들은 단점이 실행을 해봐야 에러가 나는 지를 안다
자바스크립트는 컴파일 언어가 아님

웹페이지를 동적으로 작동하기 위해 만들어졌다 자바와는 아무 관계가 없다

인라인, 임베디드, 익스터널 방식

순서 상관없이 script 먼저 실행

보통 그 안에 함수를 정의해 놓고 걔를 호출해서 사용하는 식으로 함

익스터널 끌고 들어올 때 보통 헤드 안에 데려오는 게 일반적

자스는 타입이 없어 그냥 var 하고 넣으면 그 변수가 들어가는 것

숫자를 넣으면 숫자, 문자를 넣으면 문자가 나온다

타입이 없는 언어는 겁나 짜증남

중복 선언이 가능하다
그래서 타입이 없으면 정말 개빡친다.. 어디서 변수가 어떻게 바뀌는지 알 수 없다

그래서 나온게 let과 const
let은 중복선언을 막았음

사실상 지금 var는 안 쓰는 게 좋음
근데 그래도 var 가 많이 나옴
var는 옛날 거야 . let으로 써야지 하는 게 좋아

실제로 코드상에서 는 let을 쓰는게 맞음
vs에서는 let으로 써라
콘솔에선 var 써도 돼

const는 자바의 final과 같음
변경할 내용이 없다면 const를 써줘도 됨
변경할 내용이 있다면 let
const는 바로 값을 넣어줘야 됨. 먼저 선언 안됨

console.log를 이용해 할 말을 적음. println같은 거임
이거 쓰면 콘솔창에 글이 나온다 단축키 : log+enter 


true false 값 있음
숫자 있고 파일 있고 계산 되고 사칙연산 다 되고
문자열은 더하기로 합치는 거 가능

타입이 궁금할 때 typeof 변수이름, 값 쓰면 됨

undefined 값을 할당하지 않는 변수

자바랑 다른 경우 변수 선언을 했어 값을 안 넣으면 null
자바스크립트는 null이 아니라 undefined

confirm 확인 트루, 취소 false

prompt 확인 나옴, 취소 null


==아니고 ===
==은 애매하고 ===이 정확해
그래서 == 안써 === 써야 돼


문자열 쓸 때 +안쓰고 ${변수}안녕하세요 이런 식으로 쓸 수 있음

디버깅 하는 방법

document 문서 전체
document.write('표시할 내용') --> 표시됨

공통된 코드들을 묶어서 일반화를 하면 좋음


onload 하고 쓰면 다 수행이 되고 나서 마지막에 수행이 됨







